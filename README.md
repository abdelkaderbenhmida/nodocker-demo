# nodocker-demo

Throwaway test repo: a plain FastAPI app with **no Dockerfile**, created to
verify a control-plane feature that auto-detects a FastAPI app
(`requirements.txt` + `main.py` with `app = FastAPI(...)`) and builds it
without one. Safe to delete.
